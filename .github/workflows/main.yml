name: hello-world
on: [push, pull_request]
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: |
         echo "Hello World!"
         pwd
      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9' # install the python version needed

      - name: execute py script # run main.pyadkjawnd
        run: python main.py
