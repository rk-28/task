name: My Workflow

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up environment variables
        run: echo "USER_INPUT=example" >> $GITHUB_ENV

      - name: Run Python script
        run: python my_script.py ${{ env.USER_INPUT }}
