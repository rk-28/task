name: Math Operation

on:
  push:
    branches:
      - main

jobs:
  calculate:
    runs-on: ubuntu-latest

    steps:
      - name: Perform math operation
        run: |
          X=$1
          Y=$2
          result=$((X + Y))
          echo "The result is $result"
